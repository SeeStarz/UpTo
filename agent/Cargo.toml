[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { version = "0.1.0", path = "../common" }
dirs = "6.0.0"
reqwest = { version = "0.13.4", features = ["blocking", "json"] }
serde_json = "1.0.151"
